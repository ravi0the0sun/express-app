module.exports ={
    // if the app is being deployed in the docker 
    database:'mongodb://mongo:27017/crm'
    // if the app is being deployed in localhost
    // database: 'mongodb://localhost:27017/crm',
};